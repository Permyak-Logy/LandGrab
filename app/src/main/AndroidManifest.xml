<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.pyply.games.points">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LandGrab">
        <activity android:name="ru.pyply.games.points.LobbyActivity" />
        <activity android:name="ru.pyply.games.points.SettingsGameActivity" />
        <activity android:name="ru.pyply.games.points.PlayOfflineActivity" />
        <activity android:name="ru.pyply.games.points.PlayOnlineActivity" />
        <activity android:name="ru.pyply.games.points.AccountActivity" />
        <activity android:name="ru.pyply.games.points.GameActivity" /><!-- TODO: Убрать Title Bar -->
        <activity android:name="ru.pyply.games.points.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>